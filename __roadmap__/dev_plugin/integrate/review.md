# Read-Only Review

**Goal**: Trace the merged depth-0 result for what the tests cannot prove — R01 §C4 coverage and skill quality, schema invariance, the two intentional client-visible deltas, the perf claims, and the new drift tests' behaviour on invalid inputs — and file the findings as an observation report the lead routes to fixes or the retrospective.
**Pre-conditions**:
- [ ] Working in worktree `/Users/hacker/Documents/tmp/claude-worktrees/colgrep_mcp/task-review` on branch `task/review` (created by the lead from the campaign branch **after** the six depth-0 leaves merged); this file exists there — if it does not, stop and report
- [ ] `cd server && uv run pytest` passes on the branch
**Success Gates**:
- ✅ [static] `__reports__/dev_plugin/02-observation_review_v0.md` exists with the observation front matter and a findings table (id, file:line, severity bug/risk/omission/consolidation, confidence, proposed fix)
- ✅ [run] the report's Re-observation Steps reproduce every `confirmed` finding from the scratchpad, never against a real `colgrep` and never against this repository
- ✅ [static] no file outside `__reports__/dev_plugin/` is modified on this branch
**References**: [R01 §C2, §C4, §C8](../../../__reports__/dev_plugin/00-architecture_v0.md) — the anatomy to check skills against, the fact inventory to check coverage against, the only two intentional deltas; [the consistency review](../../../__reports__/consistency/01-observation_review_v0.md) — the report shape and probe style to reuse

## Step 1: Probe and report
**Goal**: Findings a fresh agent can act on cold.
**Implementation Logic**:
Read `git log main..HEAD --stat`. Probe: (1) **Coverage** — for every bullet of R01 §C4, name the skill file and line that carries it; list omissions and any fact carried twice. (2) **Skill quality** — each `SKILL.md` ≤ 150 lines, description ≥ 80 chars stating triggers, imperative body explaining why, references linked from SKILL.md with a "when to read" hint, scripts executable with `--help`; run `bash dev/skills/landing-and-release/scripts/probe_cz_check.sh`; probe `land_branch.sh` with a nonexistent branch and `release.sh` from this (non-main) worktree and confirm both refuse. (3) **Schema invariance** — `Client.list_tools()`/`list_resources()`/`list_resource_templates()`/`list_prompts()` dumped on a detached worktree at `git rev-parse main` and on this branch; diff empty. (4) **Intentional deltas only** — with a legacy-mode client and a logging callback, count `notifications/message` per `search` (expect ≤ 1) and check `list_indexes` with 400 fake indexes stays ≤ `text_budget`; confirm no other tool's text changed on the fixture (dump each tool's text on both worktrees). (5) **Perf claims** — re-run the `perf(search)` micro-benchmarks described in the commit bodies; report whether they reproduce within 2×. (6) **Validators with invalid inputs** — in a scratch copy of `AGENTS.md`, add 10 lines and confirm `test_agents_md_stays_under_the_line_cap` fails; remove a skill name and confirm `test_agents_md_names_every_dev_skill` fails; break a `SKILL.md` front matter name and confirm the front-matter test fails; add a plugin to a scratch marketplace copy and confirm the marketplace test fails (monkeypatch `REPO_ROOT` or copy the tree into the scratchpad — never edit tracked files). (7) **Regression tests failed once** — for each `fix`/`perf` commit, check the body says the new test failed against the old code; spot-check one by reverting the code hunk in the scratchpad worktree. Write `__reports__/dev_plugin/02-observation_review_v0.md` (observation template) and add it to `__reports__/dev_plugin/README.md` under Round 02.
**Deliverables**: `__reports__/dev_plugin/02-observation_review_v0.md`, `__reports__/dev_plugin/README.md` (Round 02 entry)
**Consistency Checks**: `test -f __reports__/dev_plugin/02-observation_review_v0.md && test $(git diff --name-only main..HEAD -- . ':!__reports__' ':!__roadmap__' | wc -l) -eq 0` (expected: PASS)
**Commit**: `docs(reports): record the read-only review of the dev_plugin campaign`
