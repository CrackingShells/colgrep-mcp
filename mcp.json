{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "mcpServers": {
    "colgrep": {
      "type": "stdio",
      "command": "uv",
      "args": ["run", "--quiet", "--directory", "${PLUGIN_ROOT}/server", "colgrep-mcp"],
      "env": {
        "UV_PROJECT_ENVIRONMENT": "${PLUGIN_DATA}/venv"
      }
    }
  }
}
